<% [:notice, :error, :alert].each do |type| %>
  <% if flash[type] %>
    <div class="msg-bar alert ui-state-<%= type == :error ? 'error' : 'hightlight' %> ui-corner-all <%= type.to_s %>"><%= flash[type] %></div>
  <% end %>
<% end %>
